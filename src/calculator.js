class Calculator {
  constructor() {
    this.add = (a, b) => (a + b);
    this.substract = (a, b) => (a - b);
    this.divide = (a, b) => (a / b);
    this.multiply = (a, b) => (a * b);
  }
}

export default Calculator;
